version: "3.8"

services:
  weather-ms:
    build: .
    container_name: weather-ms
    ports:
      - "8888:8888"
    environment:
      - API_KEY=your_api_key_here
    command: python main.py
    volumes:
      - .:/app
