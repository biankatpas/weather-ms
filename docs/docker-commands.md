# Start

    `docker-compose up`

# Start (leaving the terminal free)

    `docker-compose up -d`

# Build

    `docker compose up --build`

# Stop

    `docker-compose down`

# Check logs

    `docker-compose logs`
